import Adoption from '@/components/InnerPage/Adoption';
import React from 'react';

const page = () => {
    return (
        <div>
            <Adoption />
        </div>
    );
};

export default page;